# 聆听 v0.6 Regression Report

Date: 2026-05-10
Branch: `codex/v0.6.5-release`
Scope: v0.6.5 release regression after the v0.6 interface rebuild.

## Commands

```text
npm --prefix web test
npm --prefix web run build
GOCACHE=/tmp/go-build go test ./...
```

## Result

- Frontend tests: passed, 3 files / 16 tests.
- Frontend typecheck/build: passed via `tsc -b && vite build`.
- Backend Go tests: passed for all packages.

## Coverage notes

- v0.6 interface flow tests cover global search page jump, batch-action visibility, recent-play item mapping, and library-card scan progress clamping.
- Storage tests cover recent-play item response timestamps and clearing recent plays without affecting ordinary playlists or liked tracks.
